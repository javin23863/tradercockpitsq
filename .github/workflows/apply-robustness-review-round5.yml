name: Apply Robustness Review Round Five

on:
  push:
    branches:
      - codex/merge-native-sqx-robustness-higher-precision
    paths:
      - ".github/workflows/apply-robustness-review-round5.yml"

permissions:
  contents: write

jobs:
  correct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/merge-native-sqx-robustness-higher-precision
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install clean production package
        run: python -m pip install --no-deps -e .
      - name: Apply exact fifth-round corrections
        run: python .github/scripts/apply_robustness_review_round5.py
      - name: Python syntax
        run: python -m py_compile product/tradercockpit/research_robustness.py product/tradercockpit/sqx_gateway.py tests/product/test_research_robustness.py tests/product/test_sqx_retester_gateway.py
      - name: Focused robustness and gateway regressions
        run: python -m unittest tests.product.test_research_robustness tests.product.test_research_robustness_http_boundary tests.product.test_sqx_retester_gateway -v
      - name: Full product suite
        run: python -m unittest discover -s tests/product -p 'test_*.py' -v
      - name: Frontend syntax and UI suite
        run: |
          node --check web/research-backtest-robustness.mjs
          node --check tests/research-backtest-robustness.test.mjs
          npm test
      - name: Production boundary
        run: python tools/check_production_boundary.py --root .
      - name: Remove generated test artifacts
        shell: bash
        run: |
          find product tests tools -type d -name __pycache__ -prune -exec rm -rf {} +
          rm -rf product/tradercockpit_core.egg-info
      - name: Diff scope
        shell: bash
        run: |
          set -euo pipefail
          git status --short
          unexpected=$(git status --short | awk '{print $2}' | grep -Ev '^(\.github/(scripts/apply_robustness_review_round5.py|workflows/apply-robustness-review-round5.yml)|product/tradercockpit/(research_robustness.py|sqx_gateway.py)|tests/product/(test_research_robustness.py|test_sqx_retester_gateway.py)|tests/research-backtest-robustness.test.mjs|web/research-backtest-robustness.mjs)$' || true)
          if [ -n "$unexpected" ]; then
            echo "Unexpected changed paths:"
            echo "$unexpected"
            exit 1
          fi
      - name: Commit corrected candidate
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Resolve fifth-round robustness review findings"
          git push origin HEAD:codex/merge-native-sqx-robustness-higher-precision
