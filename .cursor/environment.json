{
  "name": "TraderCockpit",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip setuptools && .venv/bin/pip install --no-deps -e .",
  "ports": [
    {
      "name": "app-server",
      "port": 4173
    }
  ]
}
